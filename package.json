{
  "name": "maconta-plast-web",
  "version": "1.0.0",
  "private": true,
  "description": "Catálogo, carrito y panel administrativo de Maconta Plast",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check main.js && node --check admin.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "express": "^5.1.0",
    "express-rate-limit": "^8.1.0",
    "helmet": "^8.1.0",
    "multer": "^2.0.2"
  }
}
